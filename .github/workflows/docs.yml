name: Docs

"on":
  workflow_run:
    workflows: ["Code Coverage"]
    types: [completed]
    branches: [main]
  workflow_dispatch: {}

permissions:
  contents: write
  actions: read

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build-and-deploy:
    runs-on: ubuntu-24.04


    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.x'

      - name: Install MkDocs
        run: |
          python -m pip install --upgrade pip
          pip install mkdocs mkdocs-material mkdocs-i18n

      - name: Install Doxygen
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y doxygen graphviz

      - name: Try download coverage HTML artifact
        uses: dawidd6/action-download-artifact@v6
        with:
          workflow: coverage.yml
          name: coverage-report-html
          path: coverage-html-temp
          if_no_artifact_found: warn
        continue-on-error: true

      - name: Build site
        run: |
          # 清理可能存在的旧覆盖率目录
          rm -rf mkdocs/docs/coverage
          
          # 清理并生成 API 文档（仅在 CI 中生成，不提交到 git）
          rm -rf mkdocs/docs/doxygen-output
          ./generate_api_docs.sh
          
          cd mkdocs
          mkdocs build -d site
          
      - name: Copy coverage HTML to site
        run: |
          # Copy coverage HTML if downloaded
          if [ -d coverage-html-temp ]; then
            echo "Copying coverage report to site/coverage/"
            mkdir -p mkdocs/site/coverage
            cp -r coverage-html-temp/* mkdocs/site/coverage/
          else
            echo "No coverage report found, skipping"
          fi

      - name: Deploy to gh-pages branch
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: mkdocs/site
          force_orphan: false
